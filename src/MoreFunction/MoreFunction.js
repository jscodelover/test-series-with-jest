const MoreFunction = {
  doubleArray(array) {
    return array.map(ele => ele * 2);
  },
  getFullName() {
    return {
      first: "Manisha",
      last: "Basra"
    };
  }
};

module.exports = MoreFunction;
